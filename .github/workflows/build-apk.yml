name: Build Android APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Установка Java
      run: |
        sudo apt-get update
        sudo apt-get install -y openjdk-17-jdk
        echo "JAVA_HOME=/usr/lib/jvm/java-17-openjdk-amd64" >> $GITHUB_ENV
        
    - name: Установка Android SDK
      run: |
        # Создаем правильную структуру директорий
        mkdir -p ~/android-sdk/cmdline-tools
        
        # Скачиваем Command Line Tools
        cd ~/android-sdk/cmdline-tools
        wget -q https://dl.google.com/android/repository/commandlinetools-linux-9477386_latest.zip
        unzip -q commandlinetools-linux-9477386_latest.zip
        
        # Перемещаем содержимое в правильную директорию (latest/)
        mv cmdline-tools latest
        
        # Настраиваем переменные окружения
        echo "ANDROID_HOME=$HOME/android-sdk" >> $GITHUB_ENV
        echo "ANDROID_SDK_ROOT=$HOME/android-sdk" >> $GITHUB_ENV
        echo "PATH=$HOME/android-sdk/cmdline-tools/latest/bin:$PATH" >> $GITHUB_ENV
        
        # Добавляем в PATH для текущего шага
        echo "$HOME/android-sdk/cmdline-tools/latest/bin" >> $GITHUB_PATH
        
    - name: Установка компонентов Android SDK
      run: |
        # Принимаем лицензии
        yes | $HOME/android-sdk/cmdline-tools/latest/bin/sdkmanager --licenses || true
        
        # Устанавливаем необходимые компоненты
        $HOME/android-sdk/cmdline-tools/latest/bin/sdkmanager --sdk_root=$HOME/android-sdk \
          "platform-tools" \
          "platforms;android-31" \
          "build-tools;31.0.0" \
          "ndk;25.1.8937393"
          
        # Добавляем пути в PATH
        echo "$HOME/android-sdk/platform-tools" >> $GITHUB_PATH
        echo "$HOME/android-sdk/build-tools/31.0.0" >> $GITHUB_PATH
        
    - name: Установка системных зависимостей
      run: |
        sudo apt-get update
        sudo apt-get install -y \
          python3-pip \
          python3-venv \
          cython3 \
          build-essential \
          git \
          zip \
          unzip \
          libffi-dev \
          libssl-dev \
          libsdl2-dev \
          libsdl2-image-dev \
          libsdl2-mixer-dev \
          libsdl2-ttf-dev \
          libjpeg-dev \
          libpng-dev \
          libpq-dev \
          libgstreamer1.0-dev \
          pkg-config \
          autoconf \
          automake \
          libtool
        
    - name: Установка Buildozer
      run: |
        pip3 install --upgrade pip
        pip3 install buildozer
        pip3 install cython==0.29.36
        pip3 install virtualenv
        
    - name: Настройка Buildozer
      run: |
        # Инициализируем buildozer если нет файла
        if [ ! -f "buildozer.spec" ]; then
          buildozer init
        fi
        
        # Создаем необходимые директории
        mkdir -p ~/.buildozer/android/platform
        mkdir -p ~/.android
        
        # Настраиваем спецификацию Buildozer
        if [ -f "buildozer.spec" ]; then
          # Обновляем пути в buildozer.spec если нужно
          sed -i "s|^android.sdk_path =.*|android.sdk_path = $HOME/android-sdk|" buildozer.spec 2>/dev/null || true
          sed -i "s|^android.ndk_path =.*|android.ndk_path = $HOME/android-sdk/ndk/25.1.8937393|" buildozer.spec 2>/dev/null || true
        fi
        
    - name: Проверка окружения
      run: |
        echo "=== Проверка окружения ==="
        echo "JAVA_HOME: $JAVA_HOME"
        echo "ANDROID_HOME: $ANDROID_HOME"
        echo "ANDROID_SDK_ROOT: $ANDROID_SDK_ROOT"
        echo ""
        echo "Содержимое Android SDK:"
        ls -la $HOME/android-sdk/
        echo ""
        echo "Java версия:"
        java -version
        echo ""
        echo "Android SDK компоненты:"
        ls -la $HOME/android-sdk/platform-tools/
        ls -la $HOME/android-sdk/build-tools/
        ls -la $HOME/android-sdk/platforms/
        
    - name: Сборка APK
      run: |
        # Экспортируем переменные окружения
        export ANDROID_HOME=$HOME/android-sdk
        export ANDROID_SDK_ROOT=$HOME/android-sdk
        export JAVA_HOME=/usr/lib/jvm/java-17-openjdk-amd64
        export PATH=$JAVA_HOME/bin:$ANDROID_HOME/cmdline-tools/latest/bin:$ANDROID_HOME/platform-tools:$ANDROID_HOME/build-tools/31.0.0:$PATH
        
        # Собираем APK
        buildozer -v android debug
        
    - name: Загрузка APK
      if: success()
      uses: actions/upload-artifact@v4
      with:
        name: wave-defender-apk
        path: bin/*.apk
        
    - name: Просмотр логов при ошибке
      if: failure()
      run: |
        echo "=== Логи ошибок ==="
        if [ -f ".buildozer/android/platform/build.log" ]; then
          tail -100 .buildozer/android/platform/build.log
        fi
        if [ -f "buildozer.spec" ]; then
          echo "=== Конфигурация buildozer.spec ==="
          grep -E "^(android\.(sdk|ndk)_path|p4a|requirements)" buildozer.spec || true
        fi