name: Build Android APK

on:
  push:
    branches: [ main, master ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    # 1. Забираем код
    - uses: actions/checkout@v4

    # 2. Setup Python 3.9
    - name: Setup Python 3.9
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'

    # 3. Setup Java 17
    - name: Setup Java 17
      uses: actions/setup-java@v4
      with:
        distribution: temurin
        java-version: '17'

    # 4. Системные зависимости
    - name: Install system dependencies
      run: |
        sudo apt update
        sudo apt install -y python3-pip python3-dev git zip unzip build-essential \
          libsdl2-dev libsdl2-image-dev libsdl2-mixer-dev libsdl2-ttf-dev \
          libpango1.0-dev libharfbuzz-dev wget unzip

    # 5. Установка Buildozer, Cython и Kivy
    - name: Install Buildozer and Kivy
      run: |
        pip install --upgrade pip
        pip install buildozer cython==0.29.33 kivy==2.1.0

    # 6. Установка Android SDK + NDK
    - name: Install Android SDK and NDK
      run: |
        export ANDROID_SDK_ROOT="$HOME/Android/Sdk"
        mkdir -p $ANDROID_SDK_ROOT/cmdline-tools
        cd $ANDROID_SDK_ROOT

        # Скачиваем Command Line Tools
        wget -q https://dl.google.com/android/repository/commandlinetools-linux-9477386_latest.zip -O cmdline-tools.zip
        unzip -q cmdline-tools.zip
        mkdir -p cmdline-tools/latest
        mv cmdline-tools/* cmdline-tools/latest/ 2>/dev/null || true
        rm -rf cmdline-tools.zip

        # Создаем устаревшую структуру для Buildozer
        mkdir -p tools/bin
        ln -sf $ANDROID_SDK_ROOT/cmdline-tools/latest/bin/sdkmanager $ANDROID_SDK_ROOT/tools/bin/sdkmanager

        # Принимаем лицензии
        yes | $ANDROID_SDK_ROOT/tools/bin/sdkmanager --licenses

        # Устанавливаем платформу, build-tools и NDK
        $ANDROID_SDK_ROOT/tools/bin/sdkmanager "platform-tools" "platforms;android-33" "build-tools;33.0.0" "ndk;25.1.8937393"

        # Определяем путь к NDK
        NDK_PATH=$(find $ANDROID_SDK_ROOT/ndk -maxdepth 1 -type d | grep 25.1.8937393 | head -1)
        echo "NDK_PATH=$NDK_PATH" >> $GITHUB_ENV
        echo "ANDROID_SDK_ROOT=$ANDROID_SDK_ROOT" >> $GITHUB_ENV
        echo "ANDROID_HOME=$ANDROID_SDK_ROOT" >> $GITHUB_ENV

    # 7. Подготовка приложения
    - name: Prepare application
      run: |
        cat > main.py <<EOF
        from kivy.app import App
        from kivy.uix.label import Label

        class TestApp(App):
            def build(self):
                return Label(text="Hello from CI Build!")

        if __name__ == '__main__':
            TestApp().run()
        EOF

        cat > buildozer.spec <<EOF
        [app]
        title = TestApp
        package.name = testapp
        package.domain = org.example
        source.dir = .
        source.include_exts = py,png,jpg,kv,atlas
        version = 1.0
        requirements = python3,kivy==2.1.0
        orientation = portrait
        android.api = 33
        android.ndk_api = 21
        android.minapi = 21
        android.archs = armeabi-v7a, arm64-v8a, x86, x86_64
        android.sdk_path = $ANDROID_SDK_ROOT
        android.ndk_path = $NDK_PATH

        [buildozer]
        log_level = 2
        android.accept_sdk_license = True
        EOF

    # 8. Сборка APK
    - name: Build APK
      run: |
        export ANDROID_SDK_ROOT="$ANDROID_SDK_ROOT"
        export ANDROID_HOME="$ANDROID_SDK_ROOT"
        buildozer android debug

    # 9. Загрузка APK
    - name: Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: android-apk
        path: bin/*.apk
        if-no-files-found: warn
